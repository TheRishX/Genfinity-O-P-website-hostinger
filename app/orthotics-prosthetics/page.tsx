import type { Metadata } from "next";
import Home from "@/app/page";
import { pageMetadata } from "@/lib/seo";

export const metadata: Metadata = pageMetadata("/orthotics-prosthetics/");

export default Home;
